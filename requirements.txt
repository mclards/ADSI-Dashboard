# ADSI Dashboard — Python Service Dependencies
# Install inside the project virtualenv:
#   python3 -m venv venv
#   ./venv/bin/pip install --upgrade pip
#   ./venv/bin/pip install -r requirements.txt
#
# Used by:
#   InverterCoreService.py  — Modbus TCP/RTU poller (port 9100)
#   ForecastCoreService.py  — Solar day-ahead forecasting engine
#   CalibratorService       — Protocol decoder & parameter maps (port 9200)

# ── Web Framework (FastAPI + ASGI server) ─────────────────────────────────────
fastapi==0.110.0
uvicorn==0.29.0
pydantic==2.6.4

# ── Modbus & Serial Communication ────────────────────────────────────────────
pymodbus==2.5.3
pyserial==3.5

# ── Numerical & Data Processing ──────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.1

# ── Machine Learning (Solar Forecast) ────────────────────────────────────────
scikit-learn==1.4.1.post1
lightgbm==4.3.0
joblib==1.3.2

# ── HTTP Client (Solcast API, cloud calls) ────────────────────────────────────
requests==2.31.0

